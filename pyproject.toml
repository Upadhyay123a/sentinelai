# SentinelAI — project metadata and tooling config.
[project]
name = "sentinelai"
version = "0.1.0"
description = "Runtime security gateway for AI agents (lethal-trifecta containment)."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
